package customers

import (
	"testing"
)

func TestInsertOk(t *testing.T) {
	// db, err := db.InitTxdb()
	// assert.Nil(t, err)

	// repo := NewRepository(db)
	// defer db.Close()

	// service := NewService(repo)

	// parsedCustomers, err := parser.ParseDataCustomers()

	// service.InsertAll(parsedCustomers)
}
